project:
  name: "mock-audit-no-sae"
  results_dir: "./runs"
  seed: 0

backend:
  type: "mock"

models:
  base:
    id_or_path: "mock-base"
  benign:
    id_or_path: "mock-benign"
  adversarial:
    id_or_path: "mock-adversarial"

dataset:
  training_jsonl: "./data/mock_training.jsonl"

interp:
  sae:
    enabled: false
  neuronpedia:
    enabled: false
